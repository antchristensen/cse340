<!-- Classification View -->

<!-- Heading -->
<% if (title) { %>
  <h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<!-- Message Block (commented for future use) -->
<%# messages() %>

<!-- Inventory Display -->
<%- grid %>
